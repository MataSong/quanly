# P1-C will import consumers and append patterns here, e.g.:
#   from core.market.consumers import MarketConsumer
#   websocket_urlpatterns = [
#       re_path(r"^ws/market/$", MarketConsumer.as_asgi()),
#   ]
#
# For now this is intentionally empty — P1-B only wires the infrastructure.
websocket_urlpatterns = []
